pipeline {
    agent any
    parameters {
        string(defaultValue: 'minor', description: 'Version type to bump (minor/patch/major)', name: 'VERSION_TYPE')
    }
    tools {
        nodejs "node"
    }
    environment {
        DOCKER_HUB_ID = 'http://64.227.122.44:8083'
    }

    stages {
        stage('Tests') {
            steps {
                script {
                    dir('Ex7 - Automation & CICD/app') {
                            sh "npm i"
                            sh "npm run test"
                    }
                }
            }
        }
        stage('Bump Version') {
            steps {
                script {
                    dir('Ex7 - Automation & CICD/app'){
                        sh "npm version ${params.VERSION_TYPE}"

                        def packageJson = readJSON file: 'package.json'
                        def packageVersion = packageJson.version

                        env.IMAGE_TAG = "${packageVersion}-${env.BUILD_NUMBER}"
                    }
                }
            }
        }
        stage('Build and Push docker image') {
            steps {
                script {
                    withDockerRegistry(credentialsId: 'docker-login', url: "${env.DOCKER_HUB_ID}") {
                        build("${env.DOCKER_HUB_ID}/myapp:${IMAGE_NAME}")
                    }
                }
            }
        }
    }
}